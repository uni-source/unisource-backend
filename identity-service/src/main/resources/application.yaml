spring:
  application:
    name: identity-service
  config:
    import: "optional:configserver:http://localhost:8088"
  zipkin:
    base-url: http://127.0.0.1:9411
management:
  tracing:
    sampling:
      probability: 1.0

